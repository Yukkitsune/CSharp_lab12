@page
@model CSharp_lab12.Pages.TourP.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tour[0].Destination)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tour[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tour[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Tour) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Destination)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TourId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TourId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TourId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
