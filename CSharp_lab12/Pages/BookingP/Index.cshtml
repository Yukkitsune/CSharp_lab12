@page
@model CSharp_lab12.Pages.BookingP.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].NumberOfPeople)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].Client)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking[0].Tour)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Booking) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfPeople)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Client.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tour.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BookingId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BookingId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BookingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
